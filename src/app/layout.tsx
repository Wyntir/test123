import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mirai Cho | Portfolio",
  description: "Full-stack developer and creative technologist building exceptional digital experiences",
  keywords: ["Mirai Cho", "portfolio", "developer", "full-stack", "web development"],
  authors: [{ name: "Mirai Cho" }],
  openGraph: {
    title: "Mirai Cho | Portfolio",
    description: "Full-stack developer and creative technologist",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
